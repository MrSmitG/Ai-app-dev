# Localmod Fast

React app for **speed**: ping every backend and race the same prompt.

## Install to a folder (Mac + Windows)
Suite → Install → pick a file path.

## Run
- Windows: `Start-Windows.bat`
- macOS: `Start-macOS.command`

UI: `http://127.0.0.1:1420/fast`
